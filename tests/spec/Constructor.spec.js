'use strict';

describe( 'Constructor:', function() {
    var constructor;

    beforeEach( function() {
        constructor = mockbox.window.modules.Constructor;
    } );

    it( 'should constructor to be defined:', function() {
        expect( constructor ).toBeDefined();
    } );

    describe( 'initialization=>', function() {

    } );

} );
